server:
  port: 8002

spring:
  application:
    name: eureka-provider-payment-feign-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7001/eureka/
  instance:
    instance-id: provider-payment-feign${server.port}
    prefer-ip-address: true
